import styled from 'styled-components';
import {
    Link
} from "react-router-dom";

export const NavButton = styled(Link)`
    color: #fff;
    &:hover {
        color: #fff;
        opacity: 0.8;
    }
`